{
    "extends": "./tsconfig.base.json", // Extend the base TypeScript configuration
    "compilerOptions": {
        "paths": {
            "src/*": [
                "./dist/*"
            ]
        },
    }
}